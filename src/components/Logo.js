import React from 'react';
import logo from '../assets/logo.jpg'; // Import the logo

const Logo = () => {
    return (
        <img src={logo} alt="DevSync Logo" style={{ width: '250px' }} />
    );
};

export default Logo;