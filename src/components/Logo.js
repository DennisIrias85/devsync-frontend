import React from 'react';
import logo from '../assets/logo.png'; // Import the logo

const Logo = () => {
    return (
        <img src={logo} alt="DevSync Logo" style={{ width: '150px' }} />
    );
};

export default Logo;